<p id="notice"><%= notice %></p>

<p>
  <b>Label:</b>
  <%= @camera.label %>
</p>

<p>
  <b>Camera:</b>
  <%= @camera.camera_id %>
</p>

<p>
  <b>Server:</b>
  <%= Server::find_by_id(@camera.server_id).label %> (#<%= @camera.server_id %>)
</p>

<table>
  <tr>
    <td>
      <%= button_to 'Edit', edit_camera_path(@warehouse), method: :get %>
    </td>
    <td>
      <%= button_to 'Back', cameras_path, method: :get %>
    </td>
  </tr>
</table>
